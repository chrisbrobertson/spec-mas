# Spec-MAS v3 Environment Variables

# =============================================================================
# AI PROVIDER CONFIGURATION
# =============================================================================

# Default AI provider: 'claude' or 'openai'
# Claude uses local CLI (requires: npm install -g @anthropic-ai/cli)
# OpenAI uses API (requires OPENAI_API_KEY below)
AI_PROVIDER=claude

# Claude CLI Configuration
# The Claude CLI handles authentication automatically via: claude config set api-key YOUR_KEY
# No need to set ANTHROPIC_API_KEY when using Claude CLI
# Just run: claude config set api-key YOUR_KEY (one-time setup)

# Claude model for CLI usage
AI_MODEL_CLAUDE=claude-3-5-sonnet-20241022

# OpenAI API Configuration (optional, for fallback or primary use)
# Get your key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# OpenAI model
# Options: gpt-4, gpt-4-turbo, gpt-3.5-turbo
AI_MODEL_OPENAI=gpt-4

# Enable automatic fallback to OpenAI if Claude CLI fails
AI_FALLBACK_ENABLED=false

# =============================================================================
# LEGACY CONFIGURATION (For backwards compatibility)
# =============================================================================

# Legacy: Direct Anthropic API key (not needed with Claude CLI)
# Only used if you manually configure scripts to use @anthropic-ai/sdk
# ANTHROPIC_API_KEY=sk-ant-api03-...

# Legacy: Claude model for SDK usage
SPECMAS_REVIEWER_MODEL=claude-3-5-sonnet-20241022

# Maximum tokens per response
# Default: 4096 (sufficient for most specs)
# Max: 8192 for detailed reviews of complex specs
SPECMAS_MAX_TOKENS=4096

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Enable verbose logging for debugging
# VERBOSE=true

# Custom approval log directory
# APPROVAL_LOG_DIR=.specmas/approvals
