# Spec-MAS environment variables (project root)

# Primary AI provider: "claude" or "openai"
AI_PROVIDER=claude

# OpenAI fallback (only used when AI_PROVIDER=openai or fallback enabled)
OPENAI_API_KEY=sk-...
AI_MODEL_OPENAI=gpt-5

# Claude CLI model alias
AI_MODEL_CLAUDE=sonnet

# Enable fallback to OpenAI when Claude CLI is unavailable
AI_FALLBACK_ENABLED=false

# Optional: legacy Anthropic SDK key (some scripts still reference it)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: pipeline budget override
SPECMAS_BUDGET=50

# Optional: Spec-MAS central server
SPECMAS_SERVER_URL=http://127.0.0.1:3333

# Optional: GitHub issue integration
GITHUB_TOKEN=ghp_...
GITHUB_REPO=owner/repo
